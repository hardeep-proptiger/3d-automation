function changeOrientation() {
    if( ( g_cSystemSpecs.strDevType == 'Mobile' ) && ( g_cSystemSpecs.strOSName == 'iOS' ) ) {
        //gyro.startTracking(function(o) {
        //    var track = 'alpha-->'+ o.alpha+' beta-->'+ o.beta+' gamma-->'+ o.gamma;
        //    document.getElementById('title')
        //        .innerHTML = track;
        //});
        //g_controls = new DeviceOrientationController( g_cRenderer.m_cCamera, g_htmlCanvas );
        //g_controls.connect();
    }
};